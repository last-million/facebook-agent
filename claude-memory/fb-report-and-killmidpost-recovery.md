---
name: fb-report-and-killmidpost-recovery
description: "FB agent auto run-report page (Prod tab, no AI) + the watchdog CPU-fix and kill-mid-post silent-loss recovery shipped 2026-06-15"
metadata: 
  node_type: memory
  type: project
  originSessionId: 80639e48-5193-4896-84b0-c65946091867
---

2026-06-15 wave on the FB agent (`C:\Users\Administrator\Desktop\facbeook agent`, port 9317). Built under brutal multi-agent verification (the verdicts repeatedly caught my own fixes being wrong — trust the verifier).

**AUTO RUN-REPORT (operator wants the last run + all runs ALWAYS in the Prod tab, from logs, NO AI):**
- `GET /api/reports/runs` → `buildRunReports()` in server.js: pure log parser (ledger `facebook-live-posts.jsonl` + governor events + `server-watchdog.log`), 60s cache, clusters posts into runs by >2h gap, per-group/comment/timing/chrome-peak/restart stats + a `reconcile` block.
- `web/report.html`: self-contained responsive page; served at `/report` via `serveReport()` which injects `__DASHBOARD_TOKEN__` (same trick as serveIndex) so its fetch to the token-gated API is authorized. Prod tab embeds it as an iframe + fullscreen link.
- GOTCHA: governor events store the name under `message`, NOT `event` (logEvent writes `{at,message,...fields}`); chrome count is field `chrome`. Merge the durable `data/audit/audit-YYYY-MM-DD.log` (events.log is a rolling 600-line buffer).

**WATCHDOG CPU-FIX (my earlier "alive-but-busy guard" was a NO-OP — events.log is written by SYNCHRONOUS fs.appendFileSync ON the event loop, so a CPU-wedged loop ALSO stops writing it; it fired 0/227 times while 4 real kills landed in a day):**
- `data/fb-server-watchdog.ps1` now decides busy-vs-frozen by OS-measured CPU (`Get-SystemCpuPercent` = Get-Counter '\Processor(_Total)\% Processor Time', 3-sample MAX so noise can't flip busy→frozen→kill; WMI fallback; 0 on double-fail→treated frozen). Unresponsive + CPU>=85% = BUSY→tolerate up to MAX_BUSY_TOLERATE=6 ticks (busy-count file); CPU<85% = FROZEN→restart. Known residual: a single-core/single-threaded main-loop wedge = ~11% total on the 9-core box → classed frozen → killed (a 3-min single-thread block IS a real freeze, so acceptable). Task `FB-Server-Watchdog` MultipleInstancesPolicy = IgnoreNew (verified — no count-file race).

**PREVENT SATURATION:** `runLiveFacebookPostScript` (the SOLE connector funnel for post/comment/approval/recovery) now gates on `waitForCpuHeadroom` (skippable via opts.skipCpuGate, never set yet) — no-op when healthy, brakes only when CPU/RAM pegged. CORES_PER_PROFILE=1.5 → machineParallelCap=3 (sticky; normalizeWorkflowState re-clamps maxConcurrentProfiles every write).

**FOLDED-COMMENT FALLBACK DISABLED** (`ENABLE_FOLDED_COMMENT_FALLBACK=false`, connector): it was 0/38 in prod (clicked a hashtag/related-post link → wrong post, safe but 0 comments + burned a profile-open each time). The proven `composerFocused` fast path handles folded posts. Pending-detection regex made multilingual (EN/FR/ES/PT/DE+Arabic).

**KILL-MID-POST SILENT-LOSS FIX (#1 hole: a watchdog kill between FB-publish and the in-process `published` record silently lost the post — uncounted/uncommented/unledgered, product claimed forever):**
- runWorker writes durable `publish_intent` BEFORE the post + `publish_intent_resolved` in a `finally` (only a hard kill skips it; intentId=random hex `key`, `message`=`<runId>|<claimHash>`).
- `reconcilePendingPublishIntentsAsync` (tick-hooked, armed-gated, single-flight, 5-min throttle, max 3/pass): for an unresolved intent, marker-scan via the EXISTING `recoverSubmittedFacebookPostUrl` (findOnly) → found = record `published` (resweep comments it, no loss) + count toward run cap. NO connector change.
- DUPLICATE-SAFETY (3 brutal-verify rounds — releasing a claim on a bad signal RE-POSTS = duplicate, the historically dangerous regression): release the claim ONLY when ALL hold — **non-approval group** + scan **NOT inconclusive** + ≥2 clean not-founds. EXACT planId+sequence only (a sequence-only fallback would scan the wrong product's marker — every tick regenerates planId). Pre-check `latestPublishedFacebookLivePostForRow` → already-recorded resolves w/o scan. `reconcileScanWasInconclusive(rec)` flags timeout (`validation.errors` has `submitted_url_recovery_connector_error`) / budget (`objects[].findOnlyBudgetExceeded`, multi-line-parsed by parseJsonLogObjects) / null → treat as unknown, NEVER release. **Approval group (o-group) NEVER auto-releases** (a landed-but-pending post is feed-invisible → release would duplicate once approved): retry ~30min (6×5min) covering the approval queue → recovered if it goes live, else `needs_manual_review` + KEEP claim. Detector unit-tested 5/5.
- Report surfaces `reconcile {pending,recovered,released,needsReview}` + banner — an interrupted post is VISIBLE, never silent.

**OVERNIGHT 2026-06-16 — run stopped at 36/100 (NOT a crash/memory/CPU issue):**
- Two more bugs found+fixed in-prod: (a) **STALL at a few posts** — per-run-claimed products were re-offered (`lastPostedAt` lags the claim) so the picker filled batches with sure-skips (`skipped_product_already_used_this_run`) and never reached unclaimed rows. Fixed: `isProductClaimedForRun` excludes claimed products in `productHasReadyAssets` (buffer reserve + plan rebuild + picker) AND the picker loop. Failed post releases its claim (retryable). Run then climbed 3→8→36. (b) **GAVE UP at 36** — the auto-resume crash-loop cap was a FLAT "3 resumes then stop" and fired even though every segment was POSTING. Root cause of the crashes: the **ixBrowser DESKTOP app itself went unstable ~03:36-03:42** (events: ixbrowser_desktop_autostarted / recovery_wait_started / focus_no_window / profile_disconnected_parked) → recovery cycles made GET / unresponsive at LOW cpu → watchdog `frozen_low_cpu` killed it → 3× → cap hit. Fix: **progress-aware cap** — a crashed segment that posted >=1 resets a zero-progress counter; only 3 CONSECUTIVE zero-progress crashes give up (abs bound 40 resumes/run) → intermittent ixBrowser hiccups no longer end a 100-run. PLUS **closeFinishedIxProfilesSweep** (tick-hooked, armed, ~2min, single-flight): tracks every opened profile (`__everOpenedIxProfiles`) and closes the ones no longer locked (finished post/comment) so finished profiles can't pile up + choke the ixBrowser desktop; skips in-use + dedicated SYL. NOTE: the ixBrowser DESKTOP crashing is an EXTERNAL factor — these fixes make prod RESILIENT to it (survive+finish) + reduce its load, but watch for repeated `frozen_low_cpu` / `ixbrowser_desktop_*` events. Report files: data/RAPPORT-run100-2026-06-16.txt.

**2026-06-16 — NO comment-recovery AT REST (operator rule reinforced):** the boot crash-scoped comment-recovery (a setTimeout ~90s after boot that ran `resweepUncommentedFacebookPostsAsync({ignoreArmedGate:true})` if lastIncompleteRun was pending) was REMOVED — it re-ran on EVERY restart and churned through commenter profiles (opened p72/74/83/87/77 → posts pending-approval → `posting_profile_group_issue` → close) = browser activity on an IDLE server, which alarmed the operator ("shadow thing, I didn't even start prod"). Operator rule: **finish owed comments WHEN PROD RUNS, never at rest.** Owed comments are still done by (1) the armed run-scoped tick resweep during a run (auto-resume keeps the runId so a resumed run finishes its own posts), and (2) stopAllExternalWork's stop-drain on STOP. A disarmed/given-up run's owed comments simply WAIT until prod is launched/resumed. Verified post-fix: chrome=0 at rest, 0 `crash_run_comment_recovery_start` after boot. (`ixbrowser_reconcile_applied` every 5min is a profile-LIST sync, NOT a browser open — fine at rest.)

**2026-06-16 — THE BIG ONE: ixBrowser DAILY OPEN-QUOTA (error 1018) is the real cap, not the profiles/groups.** A "100-post" run failed ~95% (17/100): 337/354 attempts errored, all `posting_profile_group_issue` + `profile_auto_blacklisted` (61) + `posting_profile_unblocked` (63) churn. ROOT CAUSE (multi-agent verified): the ixBrowser PLAN caps **profile-OPENS at 100/day** (account-wide). The run burned them in ~34 min (372 opens for 17 posts — each post = post-open + comment-open + approval-open + retries ≈ 4-5 opens), then every open returned **error 1018 "opening times per day used up"**. The code MISCLASSIFIED 1018: `ixBrowserError` (server.js ~17798) had no 1018 case → the generic "ixbrowser...error" string tripped `isFacebookProfileOpenOrLoginFailure` (~11731) → routed to `recordPostingProfileGroupIssue({skipProfile})` → `posting_profile_group_issue`; `autoBlacklistProfileIfNeeded`'s `isOpenInfraFailure` exempted 1004 not 1018 → blacklist; reconcile PASS 2 un-benched it as "stale bench" → churn loop no profile could ever clear. **A `connectOverCDP` reconnect to an already-open profile does NOT count as an open (1018) — only `ixBrowserRequest('profile-open')` does.** So MAX ~17-30 posts/day on this plan; 100/day needs a bigger ixBrowser plan OR fewer opens/post.
- FIXED + deployed: `isIxBrowserDailyOpenQuotaExhausted` + 1018 branch in ixBrowserError (publicError `ixbrowser_daily_open_quota_exhausted`); excluded from isFacebookProfileOpenOrLoginFailure; 1018 NEVER benches (autoBlacklist early-return) + sets a run-level PAUSE latch `operator.ixOpenCapHitUntil` (autopilotMayPostNow returns `ix_open_cap_reached`, stays armed → resumes after reset); reconcile PASS 2 won't un-bench `isAutoBlacklistedStickyBench` profiles (kills the churn). Prod-tab `buildProblemProfiles` list (excludes 1018/1004 victims). Cleanup: reverted false auto-approval on 4854 (autoEnableAdminApprovalIfPersistentFailures flipped it), cleared 643+643 1018-benches.
- OPEN-OPTIMIZATION (operator wants ~50-80/day without a plan upgrade, NO account-ban risk → keep ixBrowser, just open less): the connector ALREADY supports `payload.reuseCdpEndpoint` (skip profile-open, attach over CDP) + `payload.keepBrowserOpen` (skip browser.close) — **DORMANT, never set by server**. Plan: open a profile ONCE → drive 5 posts over the kept-open CDP session (0 extra opens) → fold comment/approval into the reused session → close once; HOLD the acquireNormalIxProfileUse lock across the whole session; **HARD CAP 2 tabs/profile** (DONE in connector ~4164, closes orphan verify/OG tabs, never the posting page) so no 201-chrome crash. Operator chose: **5 posts/open-session, max 2 tabs/profile.** Pending posts: don't wait — re-checked later by the resweep (22-min cooldown). The keep-open server wiring REWORKS the leak-prevention `finally` (server.js ~15550 close+release) → high-risk → implement + adversarial-verify + small-test BEFORE deploying. Design workflows: wr36ngvmt (1018) + wgv2m2le9 (open-opt).
- KEEP-OPEN **BUILT + VERIFIED + DEPLOYED 2026-06-16 (commit a19316a), FLAG STILL OFF** (`operator.keepOpenSessionEnabled` — operator enables+launches the test himself). Server PID after restart = 14832 (was 6788); Pinterest 59812 = PID 12452 untouched. **TWO non-obvious efficacy traps caught by brutal verification (w0811eg69, w4oq84yl7) — the mechanism being wired is NOT enough:** (1) `completeVerifiedFacebookPostWithComment` (~14429) UNCONDITIONALLY closed the publisher window + deleted its lock after every post (to free it for the different-profile comment) → the just-created keep-open session was a phantom (dead window) → post #2 fresh-opened → **0 saving**. FIX: `ready.__keepPublisherWindowOpen=__wantKeepOpen` (set ~14878) gates that close — when reusing, SKIP the close + KEEP the lock; the loop's `finally` (HOLD vs close+release) owns teardown. (2) **The 5-post reuse spans 5 SEPARATE autopilot ticks** (`runLiveFacebookPostFromPlan` RETURNS after ONE post; the loop iterates GROUPS-for-one-post, not 5 posts), and the **least-used fairness picker** (`orderReadyRowsLeastUsed`, usageByPid=postedToday*1e6+lifetime) re-sorts every tick → a just-posted profile sorts LAST → not re-picked until a full rotation. With the 4-profile pool a rotation routinely exceeds the old `KEEP_OPEN_MAX_MS`=12min → window aged out → fresh open. FIX: **raised KEEP_OPEN_MAX_MS to 40min** so all 4 profiles' windows survive to their next turn (fairness PRESERVED — we do NOT jump the queue; the alive-probe self-heals a window that died; +20min hard bound still force-reaps a stuck in-use session). Plus reaper IN-USE guard (`sess.inUse`) set before the alive-probe (closes the TOCTOU + mid-approval-leg yank) and `inUse:true` at fresh-session creation (~14980) so post #1's own 6-18min approval leg isn't reaped. **SAFETY = SOUND (proven byte-for-byte OFF=original via git diff; no leak/8-cap-breach/double-release; lock released exactly once).** **HONEST CEILING: the COMMENTER open is NOT folded** (its own `acquireNormalIxProfileUse` ~12906, sets neither reuse flag) → floor ~1 open/post → realistic **~1.3 opens/post → ~75 posts/day** (vs ~17-30 before) — HITS 50-80 without a plan upgrade. Lock budget safe for **≤~5 active profiles at concurrency 3** (peak ~7/8: held publishers + active commenters); MORE profiles → commenter hits the 8-cap 503 → defers to resweep (not lost). Folding the commenter too (→ toward 5x) needs GLOBAL_MAX_OPEN_PROFILES raised — deferred follow-up. Watch events when enabled: `ix_keepopen_publisher_held_for_reuse`, `ix_keepopen_session_held`, connector `ix_reuse` vs `ix_open`.

OPERATOR DECISION (2026-06-15): chose "build the safety fix first, I will launch prod myself" — do NOT launch prod. See [[fb-no-live-post-without-ok]]. Brutal verdict on the FIRST wedge-fix round was NO-GO unattended-100; this fix closes the last data-loss hole. Box is SHARED with Pinterest (port 59812) — never touch it. Related: [[fb-admin-approval-page-identity]], [[fb-dashboard-server-stability]], [[fb-selfdrive-batch-fixes]].
