---
name: fb-ixbrowser-sync-recovery
description: Recovery playbook when the FB agent run produces 0 posts / false-blacklists profiles after an ixBrowser profile reshuffle
metadata: 
  node_type: memory
  type: project
  originSessionId: 80639e48-5193-4896-84b0-c65946091867
---

When a prod run posts 0 and auto-blacklists profiles in sequence, the cause is usually NOT Facebook/account suspension. On 2026-06-09 a 200-run produced 0 posts with `IXBrowser profile-open error 1004: Profile Open Failed` and false-blacklisted p75/p72. Real root causes (in order of impact):

1. **Stale 2nd posting group.** `groupAssignmentData` had TWO groups — the user's `o1498765421290862` (=id 1098414320641851) AND a stale `4854972804605257`. Profiles assigned to the stale group hit `cannot_post_in_group`. Fix: consolidate `groupAssignmentData` to ONE entry for the user's group with all unique profiles merged (preserve `requiresAdminApproval`, set `sharePercent:100`). The autopilot builds slots fresh from `groupAssignmentData` each tick, so no plan re-prep needed.
2. **ixBrowser wedge** = orphaned chrome.exe sessions ixBrowser lost track of → 1004 on new opens. Fix: `POST /api/profiles/close-all`, then kill leftover `chrome.exe` (Pinterest is `msedge.exe` — never touch). Verify with a small armed test.
3. **Bloated over-bench blacklist.** `state.ixbrowser.failedProfiles` is append-only and grew to 735+ lines, but `currentlyBlockedProfilesSummary` (GET `/api/profiles/blocked`) is the ONLY authoritative blocked list — a raw line-scan double-counts stale entries. Fix: `POST /api/ixbrowser/clear-failed-profiles` (safe: un-benches automation failures, PRESERVES genuine FB suspensions) + `POST /api/ixbrowser/reconcile-profiles {force:true}` (run twice for the 2-miss removal debounce).

Ground-truth diagnostic: compare `POST /api/integrations/ixbrowser/test` (fresh total) + `/api/integrations/ixbrowser/profiles` (live ids) vs roster ids from `groupAssignmentData`. Removals ALREADY auto-sync via the per-tick reconcile (line ~8388); only profile 2 was stale, [40,41,42,43] are reserved.

SUPPLY STARVATION (run posts a few then idles): readyCount low despite many harvested records. TWO independent re-use gates both must pass: (1) `state.posting.contentSources.reuseHours` (harvested, default 26h) checked in productHasReadyAssets (~7578); (2) `state.productDiscovery.reusePostedProductAfterDays` (default 7 DAYS) via recentlyUsedProductKeys (~2463) — this 7-day gate silently overrides the harvested reuseHours. ALSO: the 7-day image-retention sweep sets `imageDeleted=true` but the image FILE often still exists on disk → productHasReadyAssets skips it. Recovery on 2026-06-09: set reuseHours=6, imageRetentionDays=30, and atomically reconciled the jsonl (set imageDeleted=false on every record whose image file still exists) → readyCount 2→12. buffer readyCount can lag one tick after such edits (recomputes via assetBufferStatus→collectProductUrlsForPosting ~2736). Per-post-unique #fb tag (harvestedHashtags now takes a 5th `uniqueSeed` arg = trackingSeed) + bare-domain stripping in cleanHarvestedPostText (URL only in comment) were deployed via restart (backup server.js.bak_20260609_ixsync). REAL volume fix = more harvest SOURCE groups (current source o38679876833911 is exhausted); recycling 12 products toward 200 is spammy/account-risky.

HARVEST NAVIGATION (2026-06-09, live-tested): the operator wanted a photo-theater WALK (open /photo/?fbid=X&set=g.{groupId}, press ArrowRight newest->older, resume from saved fbid). BUILT it (harvestGroupFeed walk + advanceToNextPhoto + harvestExtractPhoto shared extractor + per-group resume in state.posting.contentSources.harvestResume + POST /api/content-sources/harvest-now). PROVEN LIMITATION: directly navigating to /photo/?fbid does NOT open the keyboard-navigable theater/lightbox — FB only opens it on a human CLICK; direct-nav shows a static photo page inside normal FB chrome (diag labels were Home/Reels/Pages, not a theater), so ArrowRight can't advance (harvest_walk_end after 1 step). The walk auto-falls-back to the GRID method (harvestGroupFeedGrid) which WORKS: deep-scroll-until-enough-UNSEEN-tiles (caps 600 tiles/180s/8 flat) = newest->oldest + skip-seen = the functional "go to oldest" the operator wanted. Live grid runs found real new products (Coleman, Mainstays, Wonder Nation pajamas). Resume is effectively the SEEN-SET (postId list), not the fbid. "WORKS FOR ANY LANGUAGE" (2026-06-10): operator wanted PURE click-theater (NO grid fallback) working for EN/FR/AR profiles. The per-profile failures were NOT language — root cause: resolveNumericGroupIdFromPage grabbed a WRONG sidebar/suggested group id (e.g. a removed group 4854972804605257), so the tile click opened a NON-navigable single-photo view (set=p.{fbid}) instead of the swipeable group stream (set=g.{groupId}) → ArrowRight had nothing to move to → 0 products. FIX (language-independent, href-only): resolve groupId + newest fbid from the grid's OWN group-stream tiles `a[href*="/photo"][href*="set=g.(\d+)"]` (skip set=p.), and click only a non-set=p. group-stream tile; log {set, navigable} on open. PROVEN: p65 (was 0, wrong group) and p67 both opened set=g navigable and produced. Grid fallback REMOVED per operator (harvestGroupFeedGrid now dead code; walk returns empty + retries next round if theater fails). JUNK FILTER: server HARVEST_NONPRODUCT_LINK denylist (news/blog: theguardian/nytimes/kptv/etc + /news/ + /article/) blocks junk from productHasReadyAssets; connector JUNK regex extended too; purged 4 existing junk records ("free fuel" news etc). POSTING gap still open: the 38-merged group roster has NON-MEMBER profiles (73,75) that fail facebook_live_post_group_failed — auto-blacklist benches them over a run but wastes early turns; should trim roster to verified members.

SOLVED 2026-06-10: CLICK-to-open-theater WORKS — navigate to /groups/{id}/media, Playwright-CLICK the first tile (trusted click -> FB mounts the lightbox), THEN ArrowRight advances (fbid changes each step). Direct test found real products (Wonder Nation pajamas, Bluey bed) walking the theater. PARALLEL DEDUP: atomic per-product claim = `fs.openSync(claimsDir/sha1(url).claim, 'wx')` (exclusive create is atomic; first profile wins, others get EEXIST -> skip) — server makes a per-round shared claimsDir under data/harvest-claims/{runId}, passes harvestClaimsDir to every parallel connector, logs harvest_profile_took {profileId, products} for tracking, rmSync's the dir after. CAVEAT: live PARALLEL server round still hit FB flakiness (some picked profiles didn't load the group + one hung past the 3.5min walk deadline — the 6-min execFileAsync kill + ixBrowserCloseAfterUse is the backstop). PYTHON: NOT used — python is WSL-only on this box (image-selector service); browser stack is Node/Playwright bound to Windows ixBrowser (CDP 127.0.0.1:53200). A WSL-python pilot driving Windows-node connectors is fragile; the Node server is the reliable pilot/coordinator. The claim files are language-agnostic so a python tracker COULD read them if ever wanted. Connector spawned fresh per harvest so connector edits need NO server restart; server.js edits DO. Walk bounded by 3.5-min wall-clock deadline (under the 6-min connector kill) to avoid profile-leak hangs.

HARVESTED CONTENT CONTRACT v2 (operator-confirmed 2026-06-10, deployed): (1) POST body = the EXACT harvested caption with NO editing at all — `preparePostingPlan` uses `String(harvestedRec.text||"")` raw (NOT cleanHarvestedPostText), then livePostPayloadForRow appends ONLY the ≤8 unique locator #tags (harvestedHashtags(..., 8) from ogTitle/ogDescription||caption) — the tags are the marker used to locate the post for permalink capture. (2) COMMENT = TEXT + link, not just the bare url: source first comments are usually JUST the affiliate link (the only "text" near it is the COMMENTER'S NAME e.g. "Couponing with Anna" — do NOT scrape it; a walk-up-from-link heuristic wrongly grabs it), so the comment text = the EXACT product caption + " " + the exact captured shortlink (commentText at ~9176 = `${harvestedRec.text} ${firstCommentUrl}`; flows via commentTextPreview→livePostPayloadForRow 10806). So caption appears in BOTH post (+tags) and comment (+link), by design. Post-CTA (contentSourcesPostCta) is unused/dead in the harvested body + hidden in copy-mode UI. The connector's commentLead capture was tried + reverted (unreliable). Prior v1 contract below is superseded for the post-body strip + comment-bare-url parts:

HARVESTED CONTENT CONTRACT (operator-confirmed 2026-06-09, all deployed): (1) post body = EXACT harvested caption, NO length cap (cleanHarvestedPostText(text, 60000); record stores up to 60k) — only their urls/bare domains/#hashtags/see-more/CTAs stripped (url belongs in the COMMENT); (2) #tags built from the product's REAL og:title/og:description fetched from the harvested link — fetched IN the harvest browser (connector opens link in a new page of the logged-in profile, real fingerprint, no bot wall; capped 10/round, 20s, fail-soft) + server-side backfill sweep `backfillHarvestedOpenGraphAsync` per tick + manual POST /api/content-sources/og-backfill. PROVEN: plain server fetch gets bot-walled by walmrt.us/mavlynk and amzlink returns storefront junk ("Kelly's Amazon Page") — junk guard rejects storefront/bot titles, falls back to caption-derived harvestedShortTitle (which is good: sellers paste product names in captions); (3) comment = firstCommentUrl EXACT match (no lead-in phrase, never re-shortened) — harvested branch of commentText in preparePostingPlan.

DYNAMIC SYNC — DEPLOYED + PROVEN 2026-06-11 (design w01zs2onm): (a) **reconcile PASS-3 auto-ADD** live non-reserved profiles into EVERY posting group (so they post+comment without a manual Build-Matrix) — gated by `operator.autoAddNewProfilesEnabled` (default true); skips reserved 40-43 / moderator / SYL / blocked via isReservedReconcileProfile + per-group isFacebookAdminApprovalProfileLabel. (b) **1004 profile-OPEN failure no longer counts toward the blacklist** (autoBlacklistProfileIfNeeded `isOpenInfraFailure` early-return). **NEVER-REMOVE (operator hard rule):** the GONE-removal is now gated behind `operator.autoRemoveGoneProfilesEnabled` (default FALSE) — a vanished profile is KEPT in the roster indefinitely (listed as pendingRemoval for visibility, removed=0). Sync runs at BOOT (15s, force) + every 5min (heartbeat, standalone) + each armed tick; fail-closed (skips if iX fetch fails or <3 profiles). New helper `existingIxBrowserProfileRows()` (id+name). Proven boot: added=4, removed=0, pendingRemoval=9, no reserved/moderator (41/42) in the posting roster. See [[fb-multilang-composer-fix]], [[fb-agent-highscale-pipeline]].
